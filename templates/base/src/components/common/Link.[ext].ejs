import { cn } from '@/lib/utils';
<% if (router === 'react-router') { %>
import { Link as RouterLink } from 'react-router';
<% if (isTS) { %>
import type { ComponentProps } from 'react';

export function Link({ className, ...props }: ComponentProps<typeof RouterLink>) {
<% } else { %>
export function Link({ className, ...props }) {
<% } %>
  return <RouterLink className={cn('underline-offset-4 hover:underline', className)} {...props} />;
}
<% } else { %>
import { Link as RouterLink } from '@tanstack/react-router';
<% if (isTS) { %>
import type { ComponentProps } from 'react';

export function Link({ className, ...props }: ComponentProps<typeof RouterLink>) {
<% } else { %>
export function Link({ className, ...props }) {
<% } %>
  return <RouterLink className={cn('underline-offset-4 hover:underline', className)} {...props} />;
}
<% } %>
