<% if (isTS) { %>
interface Feature {
  icon: string;
  title: string;
  description: string;
}

<% } %>
const FEATURES<% if (isTS) { %>: Feature[]<% } %> = [
  { icon: '⚡', title: 'Vite + React',      description: 'Lightning-fast HMR and optimised production builds out of the box.' },
  { icon: '🎨', title: '<%= ui === "shadcn" ? "shadcn/ui" : "HeroUI" %>',        description: 'Beautiful, accessible components styled with Tailwind CSS v4.' },
  { icon: '🔒', title: 'Auth ready',        description: 'Mock AuthContext with login, signup, and role-based route guards.' },
  { icon: '📊', title: 'Dashboard',         description: 'User and Admin dashboards pre-wired with sidebar navigation.' },
  { icon: '🧪', title: 'Tests included',    description: 'Vitest + React Testing Library setup with example tests per feature.' },
  { icon: '🚀', title: 'CI / CD',           description: 'GitHub Actions workflow — lint, typecheck, test, build on every PR.' },
];

export default function FeatureSection() {
  return (
    <section className="border-t bg-muted/40 px-4 py-16">
      <div className="mx-auto max-w-5xl">
        <h2 className="mb-10 text-center text-2xl font-semibold tracking-tight sm:text-3xl">
          Everything you need to ship
        </h2>
        <div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
          {FEATURES.map(f => (
            <div key={f.title} className="rounded-lg border bg-card p-5 shadow-sm">
              <div className="mb-3 text-3xl">{f.icon}</div>
              <h3 className="mb-1 font-semibold">{f.title}</h3>
              <p className="text-sm text-muted-foreground">{f.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
