import DashboardLayout from '@/features/user-dashboard/layouts/DashboardLayout';
import { DASHBOARD_STATS } from '@/features/user-dashboard/data/stats';
import { useAuthStore } from '@/store/useAuthStore';

export default function Dashboard() {
  const user = useAuthStore(state => state.user);

  return (
    <DashboardLayout>
      <div className="space-y-6">
        <div>
          <h1 className="text-2xl font-bold tracking-tight">Dashboard</h1>
          <p className="text-muted-foreground">Welcome back, {user?.name}.</p>
        </div>

        <div className="grid gap-4 sm:grid-cols-3">
          {DASHBOARD_STATS.map(card => (
            <div key={card.label} className="rounded-lg border bg-card p-5 shadow-sm">
              <p className="text-sm text-muted-foreground">{card.label}</p>
              <p className="mt-1 text-3xl font-bold">{card.value}</p>
            </div>
          ))}
        </div>

        <div className="rounded-lg border bg-card p-5">
          <h2 className="mb-3 font-semibold">Recent activity</h2>
          <p className="text-sm text-muted-foreground">No recent activity yet. Start by creating a project.</p>
        </div>
      </div>
    </DashboardLayout>
  );
}
